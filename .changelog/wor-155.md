# WOR-155

Fixed: Convex deploy was failing because `convex/lib/compression.ts` imported
Node's `crypto` module, which is unavailable in the Convex V8 runtime. Replaced
the SHA-256 content hash with a pure-JavaScript FNV-1a hash that requires no
Node built-ins, unblocking CI and deployment.
