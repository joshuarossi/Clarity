# WOR-152

Fixed: Added missing required `role` and `formCompletedAt` properties to two
`PartyStates.self` test fixtures in `case-private-page.test.tsx`, resolving
two TS2739 type errors that were breaking the build on `main`.
