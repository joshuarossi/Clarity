## WOR-109

Added user authentication via Convex Auth with two sign-in providers:
magic link (email-based one-time login via Resend) and Google OAuth.
On first login through either provider, a user row is automatically
created with `role: "USER"`. Sessions are managed by Convex Auth
internals and persist across browser reloads. No password-based
registration is supported.
