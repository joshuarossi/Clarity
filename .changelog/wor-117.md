## WOR-117: Private coaching module

Added the private coaching Convex module (`convex/privateCoaching.ts`) with
a reactive query and two mutations that power the private coaching phase.
Each party's private messages are visible only to themselves — the
`myMessages` query enforces caller-only visibility at the database index
level. `markComplete` triggers synthesis automatically once both parties
finish coaching.
