### WOR-142 — Fix ForbiddenRedirect test assertion

Fixed a stale unit-test assertion in `case-detail-page.test.tsx` that did not
account for the error state passed to `navigate()` when a non-party user is
redirected from a case. The redirect behaviour itself was already correct; only
the test expectation was out of date.
