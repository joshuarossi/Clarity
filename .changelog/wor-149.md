# WOR-149

Fixed: Coach chat messages now render markdown formatting (headings, bold,
italic, lists) instead of displaying raw markdown source characters. A new
`MarkdownContent` component handles safe inline rendering for all
Coach/assistant messages across private coaching and joint chat.
