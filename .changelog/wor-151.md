# WOR-151

Fixed: Removed 4 unused-identifier errors (`@typescript-eslint/no-unused-vars`)
in test files so that `bun run lint` passes cleanly. The unused `userAId` /
`userBId` destructurings in `joint-chat.test.ts` and the unused
`MarkdownContent` import in `message-bubble.test.tsx` were deleted without
altering any test assertions.
