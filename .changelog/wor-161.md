# WOR-161

Fixed: The deployed app rendered completely unstyled because `src/main.tsx`
never imported `src/styles/globals.css`. Added the missing CSS import so
that all design tokens, base styles, fonts (Inter, JetBrains Mono), and
dark-mode theming are included in the Vite production build.
