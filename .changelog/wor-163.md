## WOR-163

Fixed a bug where all authenticated Convex queries and mutations returned
401 ("No user record found for email undefined"). The `requireAuth`,
`requireAdmin`, and `users.me` functions now resolve the current user by
`identity.subject` (the user's document `_id`) instead of the
always-undefined `identity.email`, restoring normal authenticated access.
