# WOR-157

Fixed: Convex schema push was failing because the `users` table defined two
indexes (`by_email` and `email`) covering the same `["email"]` field. Removed
the redundant `email` index, keeping `by_email` as the single index on that
field. This unblocks `npx convex deploy` and CI e2e tests.
