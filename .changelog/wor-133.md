# WOR-133 — Admin templates list page

Added the `/admin/templates` page where administrators can view all
coaching templates at a glance. The table displays each template's
category, name, current version number, active/archived status badge,
and pinned-cases count. Archived templates are visually muted, and an
empty-state message guides new users. Non-admin users are automatically
redirected to the dashboard.
