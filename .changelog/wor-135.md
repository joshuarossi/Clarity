## WOR-135: Admin audit log page

Added an admin-only audit log page at `/admin/audit` that displays a
filterable, paginated table of all administrative actions (actor, action,
target, timestamp). Clicking a row opens a detail drawer showing full
entry metadata as formatted JSON. Filters support actor, action type,
and date range.
