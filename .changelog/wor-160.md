# WOR-160

Fixed: Authentication was broken in deployed environments because the
required `convex/http.ts` file was missing. Without it, Convex Auth HTTP
callback routes (used by magic-link and Google OAuth sign-in) were never
registered, causing "HTTP actions not enabled" errors. Added the file to
create an HTTP router and wire auth routes, restoring end-to-end sign-in
flows in production.
