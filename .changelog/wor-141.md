## WOR-141

Fixed a contradictory test assertion in `MessageInput` where the Send
button's disabled state was checked without first populating the textarea.
The test now types text before asserting the button is enabled when
`isAiResponding` is false, aligning with the component's contract that
the Send button is disabled whenever the input is empty.
