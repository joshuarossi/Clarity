## WOR-106 — Seed data script

Added `convex/seed.ts`, an idempotent seed mutation that bootstraps a
development database with one admin user (`admin@clarity-dev.local`) and
three default templates (workplace, family, personal), each linked to an
initial published template version. The script is gated to non-production
environments and can be run via `npx convex run seed:seed`.
